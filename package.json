{
  "private": true,
  "scripts": {
    "test": "clojure -M:test",
    "test:async": "nbb --classpath src:test -e \"(require '[nekko.recipient-grant-async-test])\" && nbb --classpath src:test -e \"(require '[nekko.keyslot-test])\" && nbb --classpath src:test -e \"(require '[nekko.private-object-async-test])\"",
    "test:cljs": "clojure -M:cljs -m cljs.main --target node -d target/out -m nekko.cljs-test-runner",
    "test:portable": "nbb --classpath \"src:test:$(clojure -Spath)\" test/run_portable.cljs"
  },
  "dependencies": {
    "@noble/curves": "^1.9.7",
    "@noble/hashes": "2.0.1"
  }
}
